import styled from "styled-components";

const Container = styled.div`
  height: 100vh;
`;

const Image = styled.img`
  margin-right: 16px;
  margin-left: 16px;
  vertical-align: top;
`;

const Actions = styled.div`
  max-width: 200px;
`;

const Brand = styled.div`
  margin: auto;
`;

export { Container, Image, Actions, Brand };
